using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FirlatmaTrigger : MonoBehaviour {

	/*void OnTriggerStay(Collider col){
		//Player Onundekini Kullanmasi icin
		if (!col.isTrigger)
			return;
		if (col.transform.root.childCount > 1) {
			if(col.transform.GetComponent<PlayerController_A>() && col.transform.GetComponent<PlayerController_A>().isAI){
                //	Debug.Log ("Triggerdan_firlatma");
                if (col.transform.root.GetChild (0).GetComponent<PlayerController_A> ().kaldiririyor) {
                    if (col.transform.root.GetChild(0).GetComponent<PlayerController_A>().firlatici_kontrol)
                    {   
                        col.transform.root.GetChild(0).GetComponent<PlayerController_A>().DisardanFirlat();
                        col.transform.root.GetChild(0).GetComponent<PlayerController_A>().firlatici_kontrol = false;
                    }
				}

			}
		}

	}*/
}
